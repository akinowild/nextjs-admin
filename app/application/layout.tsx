import React from "react";

export default function NewsLayout({children}:{
    children:React.ReactNode
}){
    return (
        <section>
            <nav>新新新</nav>
            {children}
        </section>
    )
}